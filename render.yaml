services:
  - type: web
    name: senu-booking
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: CONTACT_EMAIL
        sync: false
      - key: RESEND_API_KEY
        sync: false
      - key: RESEND_FROM
        sync: false
